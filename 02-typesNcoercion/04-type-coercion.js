//> Coercion is implicit - in a dynamically and weakly typed language
/**
 * * + involves coercion to a string type when used as a binary operator and
 * * + involves coercion to a number type when used as a unary operator
 * */
var marks = 100;
console.log(`I got ${marks + ''} in the Mathematics`);
//% Number and String to the rescue
//> Conversion is explicit - When we need it, like integrations with another languages
